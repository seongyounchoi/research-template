# data

Place local datasets here.

Dataset files are ignored by Git by default to avoid committing private or large
data. Keep small schema descriptions or data notes in tracked Markdown files
when useful.
